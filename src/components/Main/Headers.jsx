const Headers = ({ title }) => {
    return (
        <h2 className="font-title-hammer text-2xl lg:text-4xl mb-3 text-right text-stone-500">
            {title}
        </h2>
    )
}

export default Headers